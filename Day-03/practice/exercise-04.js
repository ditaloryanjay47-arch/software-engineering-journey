const hasLaptop = true;
const hasInternet = true;
const finishedHTML = false;


if (hasLaptop && hasInternet) {
    console.log("Ready to Study");
} else {
    console.log("Not Ready");
}

if (finishedHTML) {
    console.log("Ready for CSS");
} else {
    console.log("Continue HTML Practice");
}